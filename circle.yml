machine:
  timezone:
    America/New_York
  node:
    version: 4.5.0

general:
  branches:
    only:
      - master
      - /feature-.*/

deployment:
  production:
    branch: master
    commands:
      - npm run deploy:prod
